{
  "compilerOptions": {
    "target": "ES2021",
    // this enables better inference for data properties on `this`
    "strict": true,
    // if using webpack 2+ or rollup, to leverage tree shaking:
    "module": "es2015",
    "moduleResolution": "node",
    "include": ["components.d.ts"]
  }
}
